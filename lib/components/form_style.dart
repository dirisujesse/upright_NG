import 'package:flutter/material.dart';

const formBrdr = const OutlineInputBorder(
  borderSide: const BorderSide(color: Colors.black),
);

const errBrdr = const OutlineInputBorder(
  borderSide: const BorderSide(color: Colors.red),
);

const formActiveBrdr = const OutlineInputBorder(
  borderSide: const BorderSide(color: Colors.black, width: 2.0),
);

const formBrdrWhite = const OutlineInputBorder(
  borderSide: const BorderSide(color: Colors.white),
);

const formActiveBrdrWhite = const OutlineInputBorder(
  borderSide: const BorderSide(color: Colors.white, width: 2.0),
);

const formBrdrUnd = const UnderlineInputBorder(
  borderSide: const BorderSide(color: Colors.black),
);

const formActiveBrdrUnd = const UnderlineInputBorder(
  borderSide: const BorderSide(color: Colors.black, width: 2.0),
);

const formBrdrWhiteUnd = const UnderlineInputBorder(
  borderSide: const BorderSide(color: Colors.white),
);

const formActiveBrdrWhiteUnd = const UnderlineInputBorder(
  borderSide: const BorderSide(color: Colors.white, width: 2.0),
);
